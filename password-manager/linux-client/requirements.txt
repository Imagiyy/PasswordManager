#
# Python dependencies for the VaultManager Linux client.
#
# IMPORTANT: PyGObject requires system packages before pip install.
# Ubuntu/Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 libgtk-4-dev
# Fedora:        sudo dnf install python3-gobject gtk4-devel
#
# Install in a virtual environment:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

argon2-cffi>=23.1.0
cryptography>=42.0.0
requests>=2.31.0
PyGObject>=3.46.0
